extern crate cards_core;
extern crate cards_translate;
use cards_core;
use cards_translate;

pub fn find_translation() {
}

#[cfg(test)]
mod tests {
    #[test]
    fn it_works() {
        assert_eq!(2 + 2, 4);
    }
}
